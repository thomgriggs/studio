const page=document.body.dataset.account;document.querySelector(`[data-page="${page}"]`)?.classList.add('active');
const d=ACCOUNT_DATA;
let saved={};try{saved=JSON.parse(localStorage.getItem('mwh-settings'))||{}}catch(e){saved={}}
if(saved.profile)Object.assign(d.profile,saved.profile);
if(saved.address)Object.assign(d.address,saved.address);
if(saved.notifications)Object.assign(d.notifications,saved.notifications);
function persist(section,values){saved[section]=Object.assign(saved[section]||{},values);localStorage.setItem('mwh-settings',JSON.stringify(saved))}
function flashSaved(button){const original=button.textContent;button.textContent='Saved';button.disabled=true;setTimeout(()=>{button.textContent=original;button.disabled=false},1400)}
const householdRows=d.household.map(person=>`<div class="household-row"><span class="person-chip ${person.chip}">${person.initials}</span><div><strong>${person.name}</strong><small>${person.role} · ${person.pieces} pieces</small></div><button>Edit</button></div>`).join('');
const allNames=d.household.map(p=>p.name.split(' ')[0]);
let visibleNames;try{const saved=JSON.parse(localStorage.getItem('mwh-visible-members'));visibleNames=Array.isArray(saved)&&saved.length?saved:allNames.slice()}catch(e){visibleNames=allNames.slice()}
const visibilityRows=d.household.map(person=>{const name=person.name.split(' ')[0];const checked=visibleNames.includes(name);return `<label class="visibility-row"><span class="person-chip ${person.chip}">${person.initials}</span><span>${person.name}<small>${person.role}${person.age?` · ${person.age}`:''}</small></span><input type="checkbox" data-visible-member="${name}" ${checked?'checked':''}></label>`}).join('');
const views={profile:{title:'Personal profile',intro:'Manage the details and wardrobe preferences connected to your membership.',body:`<form class="settings-card settings-form" id="profile-form"><label>First name<input id="profile-first" value="${d.profile.firstName}"></label><label>Last name<input id="profile-last" value="${d.profile.lastName}"></label><label class="full">Email<input id="profile-email" type="email" value="${d.profile.email}"></label><label>Phone<input id="profile-phone" value="${d.profile.phone}"></label><label>Default closet<select id="profile-closet"><option>${d.profile.defaultCloset}</option><option>Whole household</option></select></label><button type="submit">Save profile</button></form>`},household:{title:'Your household',intro:'Give each person a separate closet while keeping pickup, storage, and billing together.',body:`<div class="settings-card">${householdRows}<form class="settings-form" style="margin-top:22px"><label class="full">Invite a household member<input type="email" placeholder="Email address"></label><button>Send invitation</button></form></div><div class="settings-card" style="margin-top:18px"><h3 style="margin:0 0 4px;font:400 20px Fraunces,serif">Closet visibility</h3><p style="margin:0 0 16px;color:var(--sage);font-size:11px">Choose whose closets show up in your closet switcher.</p><div id="visibility-list">${visibilityRows}</div></div>`},membership:{title:'Membership',intro:'Review your plan, garment allowance, service area, and billing details.',body:`<div class="plan-card"><div><h3>${d.membership.plan}</h3><p>Up to ${d.membership.allowance} cataloged pieces with scheduled pickup and delivery in ${d.membership.area}.</p></div><span>ACTIVE</span></div><div class="settings-card" style="margin-top:18px"><div class="household-row"><div><strong>${d.membership.used} of ${d.membership.allowance} pieces</strong><small>${d.membership.allowance-d.membership.used} catalog spaces remaining</small></div><button>Change plan</button></div><div class="household-row"><div><strong>${d.membership.card}</strong><small>Next billing date ${d.membership.nextBilling}</small></div><button>Update</button></div></div>`},addresses:{title:'Addresses',intro:'Manage the places where we collect and return your wardrobe.',body:`<form class="settings-card settings-form" id="address-form"><label class="full">Primary address<input id="address-primary" value="${d.address.primary}"></label><label>City<input id="address-city" value="${d.address.city}"></label><label>ZIP code<input id="address-zip" value="${d.address.zip}"></label><label class="full">Delivery instructions<input id="address-instructions" value="${d.address.instructions}"></label><button type="submit">Save address</button></form>`},notifications:{title:'Notifications',intro:'Choose which wardrobe, care, and delivery updates you receive.',body:`<div class="settings-card"><div class="toggle-row"><div><strong>Pickup and delivery updates</strong><small>Timing, arrival, and completion</small></div><button class="toggle${d.notifications.pickupDelivery?' active':''}" data-notify="pickupDelivery" aria-label="Toggle pickup updates"></button></div><div class="toggle-row"><div><strong>Care completed</strong><small>Cleaning, pressing, and repairs</small></div><button class="toggle${d.notifications.careCompleted?' active':''}" data-notify="careCompleted" aria-label="Toggle care updates"></button></div><div class="toggle-row"><div><strong>Outfit reminders</strong><small>Scheduled looks and upcoming occasions</small></div><button class="toggle${d.notifications.outfitReminders?' active':''}" data-notify="outfitReminders" aria-label="Toggle outfit reminders"></button></div></div>`}};
const view=views[page]||views.profile;document.getElementById('account-content').innerHTML=`<p class="eyebrow">ACCOUNT SETTINGS</p><h2>${view.title}</h2><p class="intro">${view.intro}</p>${view.body}`;
document.querySelectorAll('[data-visible-member]').forEach(box=>box.addEventListener('change',()=>{const checked=[...document.querySelectorAll('[data-visible-member]:checked')].map(b=>b.dataset.visibleMember);localStorage.setItem('mwh-visible-members',JSON.stringify(checked.length?checked:allNames))}));
document.getElementById('profile-form')?.addEventListener('submit',event=>{event.preventDefault();persist('profile',{firstName:document.getElementById('profile-first').value,lastName:document.getElementById('profile-last').value,email:document.getElementById('profile-email').value,phone:document.getElementById('profile-phone').value,defaultCloset:document.getElementById('profile-closet').value});flashSaved(event.submitter)});
document.getElementById('address-form')?.addEventListener('submit',event=>{event.preventDefault();persist('address',{primary:document.getElementById('address-primary').value,city:document.getElementById('address-city').value,zip:document.getElementById('address-zip').value,instructions:document.getElementById('address-instructions').value});flashSaved(event.submitter)});
document.querySelectorAll('[data-notify]').forEach(btn=>btn.addEventListener('click',()=>{btn.classList.toggle('active');persist('notifications',{[btn.dataset.notify]:btn.classList.contains('active')})}));
