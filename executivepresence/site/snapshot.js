document.addEventListener('DOMContentLoaded',()=>{const canvas=document.getElementById('radarChart');if(!canvas)return;const ctx=canvas.getContext('2d');const labels=['Composure','Credibility','Clarity','Connection','Confidence','Influence','Vision','Authenticity'];const scores=[86,91,74,82,78,88,69,93];const size=canvas.width=640;canvas.height=640;const c=size/2,r=220,n=labels.length;ctx.clearRect(0,0,size,size);ctx.font='14px Open Sans, Arial';ctx.textAlign='center';ctx.textBaseline='middle';for(let level=1;level<=5;level++){ctx.beginPath();for(let i=0;i<n;i++){const a=-Math.PI/2+i*2*Math.PI/n,x=c+Math.cos(a)*r*level/5,y=c+Math.sin(a)*r*level/5;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath();ctx.strokeStyle='#dce2e9';ctx.stroke()}for(let i=0;i<n;i++){const a=-Math.PI/2+i*2*Math.PI/n;ctx.beginPath();ctx.moveTo(c,c);ctx.lineTo(c+Math.cos(a)*r,c+Math.sin(a)*r);ctx.strokeStyle='#e4e8ed';ctx.stroke();ctx.fillStyle='#4d5867';ctx.fillText(labels[i],c+Math.cos(a)*(r+52),c+Math.sin(a)*(r+38))}ctx.beginPath();scores.forEach((s,i)=>{const a=-Math.PI/2+i*2*Math.PI/n,x=c+Math.cos(a)*r*s/100,y=c+Math.sin(a)*r*s/100;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.closePath();ctx.fillStyle='rgba(55,93,152,.2)';ctx.fill();ctx.strokeStyle='#375d98';ctx.lineWidth=4;ctx.stroke();scores.forEach((s,i)=>{const a=-Math.PI/2+i*2*Math.PI/n;ctx.beginPath();ctx.arc(c+Math.cos(a)*r*s/100,c+Math.sin(a)*r*s/100,6,0,Math.PI*2);ctx.fillStyle='#e9b422';ctx.fill()})});
